{いい流れです、このまま振り返りも一緒に整理すると理解が定着します 👍 あなたの内容に合わせて、そのまま使える形で埋めました👇  ---  # 今日やったこと  * ActionPolicy（認可）の基本を理解した * ハードブロック / ソフトブロックの違いを整理した * `ActiveSupport::Duration` の役割を理解した * `.from_now` / `.ago` を使った日時計算を学んだ  ---  # 今日理解できたこと  * 認可は「コントローラに書かないでポリシーに分ける」のが重要 * ハードブロックは「処理を止める」、ソフトブロックは「見せ方で制御する」 * `1.day` は数値ではなく「Durationオブジェクト」である * `.from_now` / `.ago` は   👉 **現在時刻（Time.current）と組み合わせてTimeを返す**  ---  # まだ理解できていないこと  * `authorize!` の内部で何が起きているのか（例外の流れ） * `policy(@post)` がどこで生成されているのか * ハードブロックとソフトブロックを「どう使い分けるか」の判断基準 * Durationが内部的にどういう構造を持っているか  ---  # 次にやること  * ActionPolicyの流れを図で整理する（controller → policy → view） * `authorize!` と `allowed_to?` の違いを確認する * 実際に「編集できないユーザー」を作って挙動を検証する * `.from_now` / `.ago` を使って期限切れ判定を実装してみる  ---  # 感想など  認可まわりがかなり整理されてきて、「どこに責務を書くべきか」が見えてきた。 特に「ハードブロックとソフトブロックの違い」が理解できたのは大きい。 Railsの「ロジックを分離する思想」が少しずつ実感できてきた。  ---  もし次いくなら👇 👉 「policy(@post) の正体（内部の動き）」をやると一気に理解が深まります }
