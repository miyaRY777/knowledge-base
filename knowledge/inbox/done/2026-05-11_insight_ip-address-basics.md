{* [ ] `オクテット`とは？  👉 **8ビットを1つにまとめた単位**  **解説：** `オクテット` は、8ビットのまとまりを表す言葉です。 IPv4アドレスでは、`192.168.1.1` のようにドットで区切られた1つ1つの数字が、1オクテットです。 Railsの特別な機能名ではなく、ネットワークやデータ表現で使われる用語です。NISTでは、byteは8ビットの並びとして説明されています。 ([NIST CSRC][1])  **具体例：**  * `192.168.1.1` は4つのオクテットに分けられる * `192` が1オクテット * `168` が1オクテット * `1` が1オクテット * `1` が1オクテット  この例では、IPv4アドレスを8ビットずつ4つに分けて表すために `オクテット` が関係しています。  ---  * [ ] `1バイト`とは？  👉 **8ビットを1つにまとめたデータ量の単位**  **解説：** `1バイト` は、8ビットのまとまりです。 8ビットでは `2^8 = 256` 種類の値を表せるため、10進数では `0〜255` の範囲になります。 Railsの特別な機能名ではなく、コンピュータ全般で使われるデータ量の単位です。NISTでも、1 byte は8 bitと説明されています。 ([NIST][2])  **具体例：**  * `00000000` は1バイト * `11111111` も1バイト * `192.168.1.1` の `192` は1バイトの中に入っている値 * `192` 自体が192バイトという意味ではない  この例では、IPv4アドレスの各オクテットが `0〜255` の範囲で表される理由として `1バイト` が関係しています。  ---  * [ ] `1ビット`とは？  👉 **0か1のどちらか1つを表せる、最小のデータ単位**  **解説：** `1ビット` は、コンピュータが扱う最小単位のデータです。 1ビットでは `0` または `1` の2種類を表せます。 ビット数が増えると、`2のビット数乗` の種類を表せます。  **具体例：**  * 1ビット：`0` または `1` → `2^1 = 2` 種類 * 2ビット：`00`, `01`, `10`, `11` → `2^2 = 4` 種類 * 3ビット：`000`, `001`, `010`, `011`, `100`, `101`, `110`, `111` → `2^3 = 8` 種類 * 8ビット：`2^8 = 256` 種類 * 8ビットは、10進数では `0〜255` の値を表せる  この例では、コンピュータが情報を0と1の組み合わせで表すために `1ビット` が関係しています。  ---  * [ ] `IPv4`とは？  👉 **32ビットで表すIPアドレスの形式**  **解説：** `IPv4` は、インターネット上の機器を識別するためのIPアドレス形式です。 32ビットを8ビットずつ4つに分け、`192.168.1.1` のようにドット区切りの10進数で表します。 Railsの特別な機能名ではなく、ネットワーク全般で使われる用語です。RFC 791では、IPv4の送信元アドレス・宛先アドレスは32ビットとして定義されています。 ([datatracker.ietf.org][3])  **具体例：**  * `192.168.1.1` * `8.8.8.8` * `127.0.0.1`  この例では、ネットワーク上の機器やサーバーを識別するために `IPv4` が関係しています。  ---  * [ ] `IPv6`とは？  👉 **128ビットで表すIPアドレスの形式**  **解説：** `IPv6` は、IPv4より多くのアドレスを扱えるIPアドレス形式です。 128ビットで構成され、`2001:db8::1` のようにコロン区切りの16進数で表します。 Railsの特別な機能名ではなく、ネットワーク全般で使われる用語です。RFC 8200では、IPv6の送信元アドレス・宛先アドレスは128ビットとして定義されています。 ([datatracker.ietf.org][4])  **具体例：**  * `2001:0db8:85a3:0000:0000:8a2e:0370:7334` * `2001:db8::1` * `::1`  この例では、IPv4より多くの機器にIPアドレスを割り当てるために `IPv6` が関係しています。  ---  * [ ] `一意性`とは？  👉 **他と重ならず、1つに特定できる性質**  **解説：** `一意性` は、同じものが複数存在せず、1つだけに決まる性質です。 IPアドレスでは、通信相手を間違えないように、ネットワーク上で対象を識別できることが重要です。 Railsの特別な機能名ではありません。  **具体例：**  * 同じネットワーク内で、同じIPアドレスを2台の機器に割り当てない * `192.168.1.10` はPC、`192.168.1.11` はスマホ、のように区別する * 同じIPアドレスが重複すると、通信先が分からなくなることがある  この例では、通信相手を正しく識別するために `一意性` が関係しています。  ---  * [ ] `ネットワーク識別`とは？  👉 **IPアドレスを使って、どのネットワークに属しているかを判断すること**  **解説：** `ネットワーク識別` は、IPアドレスから「同じネットワーク内の相手か」「別ネットワークの相手か」を判断する考え方です。 同じネットワーク内なら直接通信し、別ネットワークならルーターなどを通して通信します。 Railsの特別な機能名ではありません。  **具体例：**  * `192.168.1.10` と `192.168.1.20` は、同じ家庭内LANにいる可能性がある * `192.168.1.10` から外部サイトへアクセスする場合、ルーターを通って外のネットワークへ出る * IPアドレスとサブネットマスクを使って、同じネットワークかどうかを判断する  この例では、通信先が同じネットワーク内か外部ネットワークかを判断するために `ネットワーク識別` が関係しています。  ---  * [ ] `IPアドレスとドメイン名の違い`とは？  👉 **IPアドレスはコンピュータ向けの住所、ドメイン名は人間向けの名前**  **解説：** `IPアドレス` は、コンピュータが通信相手を識別するための番号です。 `ドメイン名` は、人間が覚えやすいように付けられた名前です。 DNSによって、ドメイン名は対応するIPアドレスに変換されます。ICANNも、DNSは人間が覚えやすい文字列とIPアドレスを結びつける仕組みだと説明しています。 ([ICANN][5])  **具体例：**  * 人間は `www.runteq.jp` のようなドメイン名を入力する * DNSが、そのドメイン名に対応するIPアドレスを探す * ブラウザは、取得したIPアドレスのサーバーに接続する  この例では、人間が覚えやすい名前でWebサイトにアクセスし、実際の通信ではIPアドレスを使うために `IPアドレスとドメイン名の違い` が関係しています。  [1]: https://csrc.nist.gov/glossary/term/byte?utm_source=chatgpt.com "Byte - Glossary - NIST CSRC" [2]: https://physics.nist.gov/cuu/Units/binary.html?utm_source=chatgpt.com "Definitions of the SI units: The binary prefixes" [3]: https://datatracker.ietf.org/doc/html/rfc791?utm_source=chatgpt.com "RFC 791 - Internet Protocol" [4]: https://datatracker.ietf.org/doc/html/rfc8200?utm_source=chatgpt.com "RFC 8200 - Internet Protocol, Version 6 (IPv6) Specification" [5]: https://www.icann.org/resources/pages/what-2012-02-25-en?utm_source=chatgpt.com "What Does ICANN Do?" }
